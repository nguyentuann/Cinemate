package vn.tutorial.cinemate.core.constant.api_endpoint

object ProfileEndpoint {
    private const val PREFIX = "api/customers/profile"

    const val GET_PROFILE = PREFIX

    const val UPDATE_PROFILE = PREFIX
}