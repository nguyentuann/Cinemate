package vn.tutorial.cinemate.core.constant.api_endpoint

object HistoryEndpoint {
    private const val PREFIX = "movies/watch-history"

    const val GET_HISTORY_OF_DATE = PREFIX

    const val GET_DATES = "$PREFIX/dates"
}